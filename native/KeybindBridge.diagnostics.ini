[Diagnostics]
; 0 = exact 0.8 Lua API and no autonomous scanner/runtime
; 1 = extended action API and manifest scanner, no Lua runtime
; 2 = stage the Lua environment only; never call Lua
; 3 = stage it and probe sm.isServerMode(); never load the runtime
; 4 = native luaL_loadbuffer probe only; no runtime or GUI
; 5 = minimal tickable runtime; no input state, GUI or extensions
; 6 = complete base input/runtime logic; no GUI or extensions
; 7 = base runtime; only lazy MyGUI, native Win32 menu is disabled
; 8 = full runtime; DLL loads extensions natively, callbacks wait for player
FeatureLevel=8
